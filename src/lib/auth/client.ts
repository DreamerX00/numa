// Re-export from the AuthProvider for backward compatibility
export { useAuth, getAuthDisplayName } from '@/components/providers/AuthProvider';
export type { AuthUser } from '@/components/providers/AuthProvider';